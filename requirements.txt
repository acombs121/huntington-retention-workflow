# Runtime dependencies only -- this file is what the production Dockerfile
# installs. Test-only packages (pytest, httpx) live in requirements-dev.txt.
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
google-genai>=2.0.0
google-auth>=2.28.0
requests>=2.31.0
pydantic>=2.6.0
